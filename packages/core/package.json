{
  "name": "@chat-tools/core",
  "version": "1.0.0",
  "description": "Core types, utilities and configuration for chat-tools framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/provider": "^2.0.0",
    "@chat-tools/agent": "workspace:*",
    "@chat-tools/extensions": "workspace:*",
    "@chat-tools/storage": "workspace:*",
    "ai": "^5.0.22",
    "ollama-ai-provider-v2": "^1.1.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}